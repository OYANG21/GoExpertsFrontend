pipeline {
    agent {
        docker { image 'node:14.18.0-alpine'}
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
                
            }
        }
    }
}
